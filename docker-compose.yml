services:
  postgres:
    container_name: translator-postgres
    image: postgres:17
    env_file:
      - .env
    ports:
      - "7435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: